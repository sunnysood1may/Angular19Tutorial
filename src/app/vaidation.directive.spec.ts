import { VaidationDirective } from './vaidation.directive';

describe('VaidationDirective', () => {
  it('should create an instance', () => {
    const directive = new VaidationDirective();
    expect(directive).toBeTruthy();
  });
});
